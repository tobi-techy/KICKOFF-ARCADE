services:
  app:
    build: .
    ports:
      - "5173:5173"   # Frontend
      - "3001:3001"   # Backend API
      - "8080:8080"   # Linera faucet
      - "9001:9001"   # Shard proxy
      - "13001:13001" # Shard
    volumes:
      - .:/build
